# Shell / Bash

Implementação sem runtime de linguagem, usando `dd`, `od`, `stat`, `sha256sum`, `gzip` e `file`.

```bash
./odc.sh info ../test-vectors/hello.odc
./odc.sh verify ../test-vectors/compressible.odc
```
